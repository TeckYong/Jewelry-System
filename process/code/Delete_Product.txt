public static void removeProduct(int id) {
        for (int j = products.size() - 1; j >= 0; j--) {
            if (products(j).getProductID() == id) {
                products(products.get(j));
                break;
            }
        }
    }